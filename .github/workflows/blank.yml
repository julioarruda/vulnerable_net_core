name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:    
      - name: Check out source code
        uses: actions/checkout@v4  

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '3.1.x'

      - name: Install dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      # Verificar a disponibilidade do MSBuild
      - name: Verify MSBuild
        run: |
          msbuild -version
          where msbuild

      # Adicionar etapas adicionais aqui se necessário para configurar o MSBuild

      - name: Run FoD SAST Scan
        uses: fortify/github-action/fod-sast-scan@v1
        env:
          FOD_URL: https://trial.fortify.com
          FOD_TENANT: ${{secrets.FOD_TENANT}}
          FOD_USER: ${{secrets.FOD_USER}}
          FOD_PASSWORD: ${{secrets.FOD_PAT}}
          FOD_RELEASE: dotnet:MyRelease
          # Adicione quaisquer opções extras aqui
